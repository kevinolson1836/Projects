{
  "name": "kevins-lab",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "copy-models": "node scripts/copy-3d-models.js",
    "copy-images": "node scripts/copy-images.js",
    "dev": "npm run copy-models && npm run copy-images && astro dev",
    "start": "astro dev",
    "build": "npm run copy-models && npm run copy-images && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "astro": "^4.16.0",
    "highlight.js": "^11.10.0",
    "js-yaml": "^4.1.0",
    "marked": "^15.0.0",
    "node": "^22.22.1",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.0"
  }
}
